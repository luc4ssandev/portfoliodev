---
interface Props {
  title: string;
  href: string;
  description: string;
  portada: string;
}

const { title, href, description,portada} = Astro.props;

import { string } from "astro:schema";
import Arrow3 from "./icon/Arrow3.astro";
---

<div id="card" class="w-full flex flex-col md:flex-row items-center py-8 md:py-16 gap-4 md:gap-8">
  <img src={portada} alt="proyect-1" class="w-3/4 md:w-1/2" />
  <div>
    <h3 class="text-2xl font-semibold pb-2">{title}</h3>
    <span class="text-xl font-medium text-gray-500 ">{description}</span>
    <a
      href={href}
      class="text-[#8001ff] font-semibold text-xl flex items-center pt-4"
      >Ver más<Arrow3 />
    </a>
  </div> 
</div>
